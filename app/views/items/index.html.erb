<h1>Items</h1>
<table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Titulo</th>
      <th scope="col">Preço</th>
      <th scope="col" class="text-center">Ativo?</th>
    </tr>
  </thead>
  <tbody>
    <% @items.each do |item| %>
      <tr>
        <th scope="row"><%= item.id %></th>
        <td><%= link_to item.title, item, class: 'nav-link' %></td>
        <td>R$<%= item.price %>,00</td>
        <td class="text-center"><i class="bi bi-circle-fill <%= ativo?(item.active)%>"></i></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to "New item", new_item_path %>
